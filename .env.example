# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crypto_pipeline
DB_USER=postgres
DB_PASSWORD=your_password_here

# Binance (public endpoints — no key needed for OHLCV, but set for higher rate limits)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# CryptoPanic (news headlines)
CRYPTOPANIC_API_KEY=your_key_here
